node_modules/
.env*
!.env.example
!.env.local.example
.next/
dist/
.DS_Store
*.log
.vercel

# Backup files (data export, không push lên repo)
backup-strapi-data*.tar.gz

# Ảnh render từ PDF (rất nặng, không cần đẩy lên repo)
content-templates/extracted/

# LƯU Ý: web/content/bai-giai/*.md LÀ NGUỒN DỮ LIỆU CỦA WEBSITE — phải commit.
# content-templates/ là khu vực nháp; chạy `node scripts/consolidate-content.mjs`
# để gom về web/content/bai-giai/.

# PDFs gốc (lớn, không push)
pdfs/lop-*/*.pdf

# Claude Code settings (local)
.claude/

# Ảnh sơ đồ private
sơ đồ .claude.png
